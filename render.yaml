services:
  - type: web
    name: edonet-quick
    runtime: python
    buildCommand: ./build.sh
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 60
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
